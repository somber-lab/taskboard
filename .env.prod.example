# Copy to .env.prod and fill in the values before deploying
POSTGRES_DB=taskboard
POSTGRES_USER=taskboard
POSTGRES_PASSWORD=change_me_strong_password

# URL where the app will be served (used by backend CORS)
ALLOWED_ORIGIN=http://your-server-ip-or-domain

# Host port for the frontend (default 80)
PORT=80
